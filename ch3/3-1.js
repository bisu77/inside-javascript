// 숫자 타입
var intNum = 10;
var floatNum = 0.1;

// 문자열 타입
var singleQuoteStr = 'single quote string';
var dobuleQuoteStr = "double quote string";
var singleChar = 'a';

// 불린 타입
var bool = true;

// undefined 타입
var emptyVar;

// null 타입
var nullVar = null;

console.log(
    typeof intNum,
    typeof floatNum,
    typeof singleQuoteStr,
    typeof dobuleQuoteStr,
    typeof boolVar,
    typeof nullVar,
    typeof emptyVar
);
//number number string string undefined object undefined

/*
정수, 소수 typeof number
초기화 안 한 변수, 선언 안 된 변수 type of undefined
null 선언 변수 type of object
*/
